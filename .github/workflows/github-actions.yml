name: Release script
on:
  push:
    branches:
      - test-action
jobs:
  Release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: exec release
        run: ls
