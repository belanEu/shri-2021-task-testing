name: GitHub Actions
on: [push]
defaults:
  run:
    working-directory: ./
jobs:
  Form-Release-Data:
    runs-on: ubuntu-latest
    steps:
      - name: Form release
        run: |
          bash form_release.sh
